apiVersion: v1
kind: Service
metadata:
  name: settlement-service
  namespace: pix-microservices
  labels:
    app: settlement-service
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: settlement-service