apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: pix-microservices
  labels:
    app: transaction-service
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: transaction-service